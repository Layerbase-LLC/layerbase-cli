# Override the Layerbase cloud API base (defaults to https://layerbase.com).
# LAYERBASE_API_URL=https://layerbase.com

# Non-interactive login for CI/scripts. When set, `layerbase login` saves this
# key without prompting. Prefer interactive `layerbase login` on a workstation.
# LAYERBASE_API_KEY=
